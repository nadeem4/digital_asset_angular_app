import { createAction, props } from '@ngrx/store';

export const getData = createAction('[Digital Component] getData');

/* export const addData = createAction(
  '[Digital Component] addData',
  props<{ payload: any }>()
); */
